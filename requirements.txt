# Vercel 部署专用依赖（轻量版）
# 只包含 API 运行必需的库

# HTTP 请求
requests

# 腾讯云 COS SDK
cos-python-sdk-v5

# HTML 解析
beautifulsoup4

# 注意：不需要 fastapi/uvicorn，因为使用 Vercel Serverless Functions (BaseHTTPRequestHandler)
